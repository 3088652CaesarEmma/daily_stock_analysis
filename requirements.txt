# Core dependencies for daily stock analysis
akshare>=1.10.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
requests>=2.28.0
python-dotenv>=0.21.0
schedule>=1.1.0
loguru>=0.6.0
openpyxl>=3.0.10

# Data visualization
plotly>=5.11.0
kaleido>=0.2.1

# Email notifications
# smtplib2 is broken on Python 3.11+; using yagmail as a more maintained alternative
yagmail>=0.15.0

# Database support
# sqlite3 is part of Python's standard library, no need to install via pip

# Testing
pytest>=7.2.0
pytest-cov>=4.0.0

# Code quality
black>=22.10.0
flake8>=5.0.0
isort>=5.10.0

# Personal additions
# jupyter for interactive exploration of stock data locally
jupyter>=1.0.0
